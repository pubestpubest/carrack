NODE_ENV=production
PORT=3000

# Supabase — also passed as Docker build args in GitHub Actions
# Server components read these at runtime; client bundle has them baked in at build time
NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT_ID.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
